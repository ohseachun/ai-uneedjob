import dotenv from 'dotenv';

// 환경 변수 로드
dotenv.config();

console.log('LangChain TypeScript 프로젝트가 시작되었습니다!');

// 여기에 메인 애플리케이션 로직을 추가하세요
async function main() {
    try {
        console.log('애플리케이션 초기화 중...');
        // LangChain 관련 코드를 여기에 추가
    } catch (error) {
        console.error('오류 발생:', error);
    }
}

main(); 